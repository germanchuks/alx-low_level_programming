#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: 'Prints some string using printf'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char Mystr[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", Mystr);

	return (0);
}
