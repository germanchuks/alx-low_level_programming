#include "main.h"
#include <stdlib.h>
/**
 * _abs - Entry point
 *
 * Description: 'Computes absolute value of an integer'
 * @val: 'Integer to be computed'
 * Return: Always 0 (Success)
 */
int _abs(int val)
{
	return (abs(val));
}
