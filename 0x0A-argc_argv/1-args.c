#include <stdio.h>
/**
 * main - Prints the program name followed by a new line
 * @argc: Argument count.
 * @argv: Argument vector.
 * 
 * Return: Always 0.
 */
int main(int argc, char **argv)
{
	(void)argv;
	printf("%d\n", argc);
	return (0);
}
